# ============================================================
# TriageAI — Python Dependencies
# ============================================================

# --- LiveKit Agents (Voice Room) ---
livekit-agents>=0.12.0
livekit-plugins-groq>=0.3.0         # STT: Groq Whisper
livekit-plugins-cartesia>=0.3.0     # TTS: Cartesia Sonic
livekit-plugins-silero>=0.7.0       # VAD: Voice Activity Detection

# --- LangGraph & LangChain (Orchestration) ---
langgraph>=0.2.0
langchain>=0.3.0
langchain-core>=0.3.0
# langchain-anthropic removed
langchain-pinecone>=0.2.0
langsmith>=0.2.0

# --- FastAPI Backend ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0
python-multipart>=0.0.20

# --- Database (PostgreSQL async) ---
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.30.0
alembic>=1.14.0

# --- Cache (Redis) ---
redis[asyncio]>=5.2.0

# --- AI Services ---
pinecone>=5.0.0

# --- Follow-up Services ---
twilio>=9.3.0

# --- HTTP Clients ---
httpx>=0.28.0
aiohttp>=3.11.0

# --- Data Processing (KB ingestion) ---
pandas>=2.2.0
numpy>=2.1.0

# --- Utilities ---
python-dotenv>=1.0.0
tenacity>=9.0.0
structlog>=24.4.0

# --- Testing ---
pytest>=8.3.0
pytest-asyncio>=0.24.0
pytest-mock>=3.14.0
